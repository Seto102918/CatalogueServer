import React from 'react';
import ReactDOM from 'react-dom/client';

// {{!-- import Add from './static/js/add'; --}}
const root = ReactDOM.createRoot(document.getElementById('root'));

const Add = () => {
    return (
        <div>
        <h1>  Faq Faq </h1>
        </div>
    );
}

root.render(
    <React.StrictMode>
        <Add />
    </React.StrictMode>
);